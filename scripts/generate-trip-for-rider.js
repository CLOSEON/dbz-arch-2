const admin = require('firebase-admin');
const fs = require('fs');

if (!admin.apps.length) {
  const serviceAccount = require('../../dabzofb-a15d7870932c.json'); // Or whatever the SA key is, wait let me check if there's a key
}
